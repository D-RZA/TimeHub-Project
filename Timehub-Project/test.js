let mapsss = [1, 2, 3, 4, 45];

let got = mapsss.map(maps => {
  return maps * 2;
});

console.log(got);

const numb = [4,6,8,10];

const doubed = numb.filter(numt => numt >= 5);

console.log(doubed)


lets become javascript

variable car holds all the properties of the car, we declared info and passed the key of model into ImageTrack

console.log(car.info); //js can't find the property of info
console.log(car[info]); //output camry, it looks at the variable info and finds property model in it then it's value, camry